
type Nickname = string;

type Player = {
    nickname: Nickname,
    healthBar: number,
}
const nico:Player = {
    nickname: "nico",
    healthBar: 10
}

type Food = string;
const kimchi:Food = "delicious";
